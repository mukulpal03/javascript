"use strict"; // treat all JS code as newer version

// alert(3+3) // we are using nodejs, not browser

console.log(3
    +
    3) // dont write like this, readability is important 

console.log(3+3); // good way

let name = "mukul" // string
let age = 20 // number 
let isLoggedIn = true // bool
let State; // undefined

// null => standalone value, empty value

// undefined => the value is not assigned 

// symbol => unique

console.log(typeof name);

console.log(typeof null); // null is an object

console.log(typeof undefined); // undefined is a type itself.